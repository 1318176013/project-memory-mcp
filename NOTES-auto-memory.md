# 临时讨论笔记：自动知识沉淀（读 + 写闭环）

> 状态：讨论中（草稿）。本文件是临时笔记，用于记录方案共识，避免多轮讨论后遗忘。
> 定稿后应迁移到正式文档 / 实现，再删除本文件。

---

## 1. 背景与定位（已达成共识）

这个 MCP 的定位**收敛**为「**项目认知层**」，而不是「代码索引库」。

原因：
- agent（Claude Code）**自己就能读源码、grep、看文件树**，找代码是它的强项。
- 系统源码可能上百万行，全量索引进 MCP 不现实：embedding 成本高、向量库膨胀、代码一改就要重新同步、向量搜索对代码本就不如 grep/LSP 精确。
- 所以**源码归 agent 自己读，MCP 只负责「代码之外、agent 读不到」的知识**。

职责划分：
```
agent 自己做：读源码、grep、看文件
MCP 负责（代码里读不到的）：
  ├─ 决策 / 取舍（选了 A 否了 B，为什么）
  ├─ 约定 / 规范（命名、错误处理、目录习惯）
  ├─ 踩坑 / 教训（看着能改、改了会炸 X）
  ├─ 标准写法 / 示例代码
  └─ 文档片段
  以上全部：可语义搜 + 置信度 + 图谱关联
```

判断一条知识该不该进 MCP 的**一句话标准**：
> 「如果下一个 agent 下周接手这个项目时会希望提前知道，而且它在源码里读不到 → 才记。」

## 2. 关键结论：不做代码全量索引

- 原来列的三个待解决问题（① 索引无 HTTP 入口 ② 索引单项目配置 ③ embedding 全局共享）
  在这个新定位下**紧迫性大幅下降**。
- 索引能力**最多**用于「少量高价值文档」（架构文档/ADR/README），且当前阶段**不优先做**。
- 主菜是 `add_memory`：文档片段、示例写法、最佳实践都直接进 memory。

## 3. add_memory 使用约定（待细化）

- memory = 通用知识载体，不只是"一句话笔记"。文档/示例代码都可以放。
- 一条 memory = 一个完整自洽的知识点；长文档拆成多条，每条一个主题。
- title 写好很关键：title 参与 embedding（`${title}\n\n${content}`），影响被搜中概率。
  纯语义检索下这是召回质量的命门 → **title 必须清晰、自带关键词**（见 §5）。
- 示例代码这类"稳定事实"：过时了要 `update_memory` 更新，而不是等反馈打低分。

### kind 取值（已定稿 ✅，共 5 个，少而稳）

原则：kind 少而互斥。纯语义检索下 kind **几乎不参与召回**（召回靠 title/content 语义），
它的作用只是：① 给 agent 沉淀时一个心智框架；② 将来 list_memories 按类浏览。
所以**不必追求归类 100% 准**——归错最多让浏览别扭，不会让 agent 漏掉知识。

| kind | 装什么 | 一句话判据 |
|------|--------|-----------|
| `decision` | 做了取舍的决策 + 为什么 | "我们**选了**这个，否了那个" |
| `convention` | 项目约定/规范/要求/定义 | "就**得这么做**"（命名、错误处理、目录…） |
| `pattern` | 标准写法 / 示例代码 | "要写这类代码，**照这个**" |
| `gotcha` | 坑 / 雷区 / 不能动的地方 | "**小心**，这里改了会炸" |
| `note` | 其它（背景、说明、杂项），兜底 | 前四类都不沾 |

相比早期版本的收敛（及理由）：
- 去掉 `doc`：「文档」是**来源**不是**类型**——文档片段本质要么是 convention 要么是 note。
  **按知识的"性质"分，不按"来源"分。**
- `pattern` 与 `example` 合并为 `pattern`（标准写法=示例，一回事）。
- 保留 `note` 兜底（也是 add_memory 当前默认值，server.ts:159）。

### "一条只归一类"纪律（已定稿 ✅）

真实知识天然跨类（"带原因的约定""带教训的写法"很常见）。处理办法不是允许多类，而是：

```
一条 memory 只归一个 kind，按"首要意图"选：
  约束行为→convention  提供写法→pattern  警示风险→gotcha  记录权衡→decision  其它→note
理由、背景、教训、注意事项 → 写进 content，不因为带了它们就改 kind。
若一条里有两个【各自需要被独立搜到】的无关知识点 → 拆成两条，各归各类。
```

关键判据：
- 分类时问"这条知识**首要**想让 agent 做什么"，理由/教训/背景全塞进 content。
- 拆条判据：**如果 agent 可能只想搜到其中一半，就该拆。**
- 例："错误码用 problem+json（因为之前各写各的）" → 首要是"遵守规矩" → convention，
  原因写进 content，不因带了原因就变 decision。

## 4. 写侧方案：Stop hook 强制沉淀（已确认）

机制：
```
agent 说"完成" → Stop hook 触发
  → 返回 {"decision":"block","reason":<沉淀规则>}
  → agent 被打回：先 search_knowledge 查重 → add_memory/update_memory → 回显沉淀记录
  → agent 再说"完成" → hook 再触发，但 stop_hook_active=true → 放行，真正结束
```
- `stop_hook_active` 防死循环：第二次进来为 true，直接放行。

**已确认的决定：**
- ✅ 直接上 Stop hook 强制，**不要人工确认**。
- ✅ **加过滤**：只在"本次会话有代码改动（transcript 里有 Edit/Write 调用）"时才打回，
  纯问答 / 闲聊轮直接放行，避免每轮都触发太频繁、太耗 token。
- ✅ **必须回显沉淀记录**（透明可审计），格式见下。
- ✅ Codex 暂不管，后期需要再按 Claude Code 提示词改造。

写侧沉淀边界：
- 该记：决策取舍 / 踩坑教训 / 项目约定 / 标准写法（且源码里读不到、跨会话有用）
- 不该记：源码里的事实 / 仅本次对话的临时信息 / 通用知识 / 未经验证的猜测

防重复：
- 系统已有**指纹去重**（findActiveMemoryByFingerprint）：完全相同 title+content+kind 会被挡，返回 duplicate:true。
- 但挡不住**语义重复**（换个说法的同一件事）→ 靠"先 search_knowledge 查再写"这一步补。
  - 已存在且过时 → update_memory
  - 已存在且仍有效 → 跳过
  - 不存在 → add_memory

**沉淀记录回显格式（已定稿 ✅）**

目标：不人工确认，但要能①可扫读 ②可追溯（带 ID）③审计"没记"（无沉淀也显式说）。
设计取舍：默认简洁，仅"新增"带一行摘要（让你扫一眼当场判断记得对不对）；
完整 content 不回显（已在库里，凭 ID 可查）。

有沉淀时：
```
🧠 知识沉淀
✅ 新增 [gotcha] 订单状态机不能跳过 paid 状态 (mem_a1b2)
        └ 从 created 直接改 shipped 会导致对账漏单
✅ 新增 [convention] 分页统一用游标，禁用 offset (mem_c3d4)
        └ offset 在大表上会拖垮 DB
🔄 更新 [convention] API 错误统一用 problem+json (mem_d4e5)
⏭️ 跳过 [decision] 用 Redis 做幂等 — 已存在且仍有效
```
> 注：**跳过项不带 ID**（已定稿修正）。跳过的是库里已存在项，agent 只是 search 看到"有"，
> 未必持有该条 id；强求回填 id 会逼它多查一次。故跳过仅显示 `[kind] title — 原因`，不带 id。
> （新增/更新必须带 id：它们是 agent 本次写入的，返回值里就有。）

无沉淀时：
```
🧠 知识沉淀：本次无新知识（常规改动，无新决策/约定/教训/写法）
```

规则：
- 每条一行：`图标 动作 [kind] title (ID)`
- **仅"新增"**多带一行摘要：`        └ 一句话原因/要点`；更新/跳过不带
- 四种状态图标：✅ 新增 / 🔄 更新 / ⏭️ 跳过 / 🧠 无新知识
- 跳过也要显示——证明 agent 做了查重（search_knowledge），不是盲目重复写
- ID 必须有——"不人工确认"的安全垫：事后发现记错，凭 ID 直接 update/archive

## 4d. 过滤判定逻辑（已定稿 ✅，实现照此写）

判定"本次该不该打回沉淀"。核心：**只扫"本回合"，不扫整个会话。**

放行逻辑（满足任一即放行，都不满足才打回）：
```
1. stop_hook_active === true        → 放行（防死循环：被打回后的二次 Stop，沉淀已做完）
2. 工作空间根无 .project-memory      → 放行（非接入项目，见 §7b）
3. 本回合 transcript 无 Edit/Write/MultiEdit/NotebookEdit → 放行（纯问答/闲聊轮）
4. 以上都不满足                      → 打回沉淀
```

为什么是"本回合"而不是"整个会话"：
```
扫整个会话（错）：
  回合1 改代码→完成。Stop→有编辑→打回。✓
  回合2 用户说"谢谢"。Stop→整会话"仍有"回合1的编辑→又打回！✗ 闲聊也被逼沉淀
只扫本回合（对）：
  回合2 自"谢谢"起无任何编辑→放行。✓
```

- "本回合" = 自**上一条真实用户消息**起，到当前 Stop 为止。
- **实现坑**：transcript 里 `tool_result` 也是 `role:user`，要和"真实用户输入"区分
  （真实用户消息 content 是文本，不是 tool_result），才能正确定位本回合起点。

接受的冗余（已确认 ✅）：
- 若 agent 在大任务**中途**停下问问题（也算一次 Stop）且此前已改过代码 → 会先被要求沉淀一次，
  任务继续改、结束再沉淀一次 → 同一任务可能沉淀 2 次。
- **接受**：Stop hook 无法区分"做完了"和"中途问问题"；实践中"改完即结束"占绝大多数；
  多沉淀一次顶多多花点 token，且查重（search_knowledge + 指纹去重）会挡重复写。
  强行消除要引入"任务是否真结束"的复杂判断，不值。

## 4e. 内容分配：读侧常驻 + 写侧按需注入（已定稿 ✅）

核实源码（src/mcp/server.ts）后定的分配决策——决定每样东西进不进每轮 context：

| 文件 | 进每轮 context？ | 放什么 |
|---|---|---|
| `~/.claude/CLAUDE.md` | **每轮常驻** | **只放读侧引导**（极短，见下方草案） |
| `~/.claude/hooks/memory-sink.js` | **不进** | 事件触发；只有它"打回那一刻"的 reason 注入一次 |
| `~/.claude/settings.json` | **不进** | 纯配置，注册 Stop hook |
| 各项目根 `CLAUDE.md` | 进入该项目时 | 各项目自己的铁律（与本 MCP 无关） |
| 工作空间根 `.project-memory` | **不进** | hook 读它；agent 按读侧提示去读它取 projectId |

核心优化：**写侧重型规范（kind 速查 / 一条只归一类 / 回显格式 / 沉淀边界）不放常驻 CLAUDE.md**，
全塞进 hook 打回时的 reason 按需注入，用完即走。于是每轮常驻的只有读侧几行；
写侧规范只在真要沉淀那一刻出现一次，不让纯闲聊轮也背着几百行规范。

工具签名核实结论（src/mcp/server.ts）——所有工具都需 projectId：
```
search_knowledge(projectId, query, topK?)
add_memory(projectId, title, content, kind="note", tags?, source?, allowDuplicate?)
update_memory(projectId, id, title?, content?, kind?, tags?, source?)
archive_memory(projectId, id)      ← 是 archive 不是 delete，§4"凭 ID update/archive"对得上
register_project(name?) → 返回 projectId
```
由此：写侧 hook 自己读 `.project-memory` 拿 projectId，直接拼进 reason 给 agent（省得 agent 再读）；
读侧无 hook 触发，靠 **agent 自己读项目根 `.project-memory` 拿 projectId**（已写进读侧提示词）。

## 4f. hook 打回 reason 文本（逐字稿，已定稿 ✅）

> 两个设计选择已定：A=**轻查重**（给三分支但不强制逐条 search，靠指纹去重兜底）；
> B=**带精简 kind 速查表**（每 kind 一行判据，无例子）。`<projectId>` 由 hook 自动填入。

```
🧠 本次会话有代码改动。在结束前，请把这次产生的、跨会话有用的项目知识沉淀到 project-memory。

【projectId】<由 hook 自动填入，如 prj_xxxx>

━━ 该记什么（满足：源码里读不到 + 跨会话有用） ━━
· 决策取舍（选了 A 否了 B 及原因）
· 项目约定 / 规范（命名、错误处理、目录习惯…）
· 踩坑教训（看着能改、改了会炸的地方）
· 标准写法 / 示例代码

【不该记】源码里就有的事实 / 仅本次对话的临时信息 / 通用常识 / 未经验证的猜测。
若本次确实没有上述新知识，直接走"无沉淀"回显，不要硬凑。

━━ 怎么分类（一条只归一个 kind，按"首要意图"选） ━━
· decision   —— 记录一次做了取舍的决策
· convention —— 约束"就得这么做"（规范/要求/定义）
· pattern    —— 提供可照抄的标准写法/示例
· gotcha     —— 警示风险、不能动的地方
· note       —— 以上都不沾的兜底
理由/背景/教训写进 content，不因带了它们就改 kind。
若一条里有两个各自需要被独立搜到的知识点 → 拆成两条。
title 必须清晰、自带关键词（便于将来语义搜到），如"前端表单校验约定"而非"校验说明"。

━━ 怎么做 ━━
1. 沉淀前先用关键词调 search_knowledge(projectId, query) 看是否已有同类知识：
     已存在且过时 → update_memory（更新）
     已存在且仍有效 → 跳过
     不存在 → add_memory（新增）
2. 写入时带上上面的 projectId 和选好的 kind。

━━ 最后必须回显沉淀记录（供审计，格式照抄） ━━
有沉淀时：
🧠 知识沉淀
✅ 新增 [kind] <title> (<id>)
        └ <一句话原因/要点>           ← 仅"新增"带这行
🔄 更新 [kind] <title> (<id>)
⏭️ 跳过 [kind] <title> — 已存在且仍有效

无沉淀时：
🧠 知识沉淀：本次无新知识（常规改动，无新决策/约定/教训/写法）

回显完成后即可结束，无需再做其它。
```

读侧常驻提示词（`~/.claude/CLAUDE.md`，精简版，已定稿 ✅）：
```
## 项目记忆(project-memory MCP)

开始编码任务前，若任务涉及【改已有功能 / 要守项目约定 / 易踩坑区域】：
1. 读工作空间根的 `.project-memory`(JSON)，取出 projectId；读不到则跳过本节。
2. 用该 projectId 调 search_knowledge(projectId, query) 查相关的决策/约定/教训/写法。
3. 动手前用一句话说明：将遵循哪条已查到的约定 / 避开哪个坑。

纯新建、与项目历史无关的小改动可跳过。
```

## 4b. memory 的价值定位（重要修正，已达成共识）

memory 的价值有**两层**，第二层是高频主力，之前低估了：

| | 解决的问题 | 例子 |
|---|-----------|------|
| 第一层 | agent **读不到**（源码里根本没有） | 为什么选 A 否了 B、业务/产品背景 |
| **第二层（主力）** | agent **记不住**（读得到/被告知过，但跨会话就忘） | 项目约定、各种定义、要求、示例写法 |

核心洞察：**人能把"我们项目就是这么干的"内化成记忆，但 AI 没有跨会话长期记忆**，
每次新会话都从零开始，所以同样的约定要反复纠正、纠正完下次又犯。
→ memory = 给 agent 装一个**会自我更新的外置长期记忆**。这才是这个系统不可替代的本质：
  **不是替代 grep（找代码），是补 agent 缺失的长期记忆（记规矩）。**

这让整套设计更站得住：
- 写侧 Stop hook = 把这次确认的约定固化，不随会话蒸发
- 读侧提示词 = 每次开工前先"回忆"项目规矩
- 置信度 = 约定会变（新规范取代旧规范），记忆要能更新/淘汰，不是只读文档

## 4c. 知识图谱：放弃专门建设（已达成共识）

结论：**在"agent 能读源码 + 主打 memory"的定位下，图谱性价比太低，放弃专门建设。**

理由：
- **代码结构图谱（文件调用/依赖）**：明确无意义。agent 有 grep/LSP，现场查又快又准、
  永不过期。建一套会过时的图重复 agent 已有能力 = 纯负债。
- **知识关联图谱（memory↔memory / memory↔文件）**：价值存在但撑不起成本——
  - 向量检索已覆盖大部分"相关性"（相关的 memory 通常语义也相近，会一起命中）；
  - 边要 agent 持续维护（连错/漏/过时都要管），噪音风险高；
  - 要有用需一定密度，养密度的投入产出比差。
- **更扎心**：图谱的杀手级场景恰恰是"agent 读不到源码时"——而前提是 agent 能读源码，
  抽掉了图谱最硬的立足点。

处理方式：
- 放弃专门建设关联图谱（不做 add_relation 引导、不建文件节点）。
- 保留 add_memory 自带的 tag→concept（零成本副产品，不专门维护）。
- 现状补充：图谱目前只在「索引代码」和「add_memory(tag)」时写入；既然不索引代码，
  代码结构图谱本就不会产生。

## 5. 读侧方案：纯语义检索 + 少量全局铁律常驻（已确认 ✅ 简化版）

> 重要：本节经历过一轮"复杂化又简化回来"。曾设计过【三层 tag 加载】方案
> （全局铁律常驻 / 领域约定按 tag 精筛 / 长尾语义搜 + 受控 tag 词表），
> **已否决——对当前阶段过度设计**。最终采用下面的简单方案。

为什么纯语义检索够用：
- 主诉求是"agent 老忘约定"。search_knowledge 基本能治：搜"前端 登录"就命中相关约定。
- 担心的"改前端会不会把后端/数据库约定也召回"：向量相似度天然过滤——无关领域语义不相近，
  排序靠后/捞不上来。偶尔混进一两条无关的，agent 扫一眼就知道不适用，**危害远小于 tag 体系的复杂度**。

纯语义检索唯一真风险 + 廉价补救：
- 真风险不是"多召回无关的"，而是"**漏召回该遵守的**"——尤其"写任何代码都要遵守、
  但和当前 query 语义不像"的项目铁律（如"提交前必须跑 lint"）。
- 补救（不需要 tag 体系）：**项目铁律由各项目写进自己的 `CLAUDE.md` 常驻**
  （Claude Code 原生自动加载项目根 CLAUDE.md），其余一切走纯语义 search_knowledge。

> **铁律不归我们管（已达成共识）**：铁律是各项目自己的业务规矩，本 MCP 项目无权也无力替别人定。
> 我们只提供**机制**，不提供**内容**。而且铁律根本不需要我们做事——各项目把自己"写代码必守"的
> 硬规矩写进**自己项目根的 CLAUDE.md**（原生机制，常驻，零成本，与我们无关）。
> 因此：① §7 清单里的"全局铁律清单"删除；② §9 对应 TODO 关闭；③ 我们的全局
> `~/.claude/CLAUDE.md` 收敛为**纯机制说明书**（读侧引导 + kind 规范 + 沉淀边界 + 回显格式），
> 项目无关、无状态、永不需改，与 §7b"全局规则层=只读消费者"的定位一致。

最终方案（简单版）：

| | 怎么做 |
|---|--------|
| 项目铁律 | **各项目自己**写进**自己项目根的 CLAUDE.md**（原生机制，与本 MCP 无关） |
| 其余所有知识（约定/示例/教训/决策） | 纯 search_knowledge 语义检索，要用才捞 |
| 读侧提示词 | "开工前先 search_knowledge 查相关约定/示例/教训，再动手" |
| 写侧 Stop hook | 不变（沉淀 + 查重 + 回显） |
| **tag** | **可选，不强制**。打了将来能用，不打也不影响检索 |

砍掉的东西（明确不做）：tag 受控词表、三层加载、按领域筛、按 kind 全量加载。

唯一要接受的取舍 → 转化成一条写侧硬要求：
- 纯语义检索下，召回质量 100% 依赖 query 和 memory 的 title/content 写得清不清晰。
- 所以**写侧沉淀时 title 必须清晰、自带关键词**（如"前端表单校验约定"而非"校验说明"），
  这样 agent 搜得到。这比维护 tag 词表轻得多——只是"把标题写好"。

演进策略：**先简单跑起来，真用一段时间后若发现某类约定老搜不准/老漏，再针对那一类补 tag。
让复杂度在出现真实问题时才引入，不预先优化（现在连数据都没攒，提前上 tag 没依据）。**

读侧提示词草案：
```
开始一个编码任务前，如果任务涉及：
  - 修改/扩展已有功能
  - 要遵循项目约定的地方（命名、错误处理、目录结构…）
  - 容易踩坑的区域
那么先调用 search_knowledge / get_context 查一下 project-memory，
看是否有相关的决策、约定、教训、标准写法，再动手。

纯新建、与项目历史无关的小改动，可跳过。
查到相关知识时，在动手前简要说明你将遵循哪条已有约定/避开哪个坑。
```
- 最后一句（说明将遵循哪条）很重要：逼 agent 真的用上查到的知识，是读侧的质量保证，
  对应写侧的"回显沉淀记录"。

## 6. 最终形态

| | 时机 | 机制 | 强度 |
|---|------|------|------|
| 读 | 任务开始 | CLAUDE.md 提示词 | 软引导（agent 自判） |
| 写 | 任务结束 | Stop hook（带代码改动过滤） | 强制 |

## 7. 待产出的落地清单

1. `.claude/settings.json` — 注册 Stop hook
2. `.claude/hooks/memory-sink.js` — 写侧：读 transcript 过滤（无 Edit/Write 放行）
   + 防死循环 + 打回沉淀规则
3. `~/.claude/CLAUDE.md`（或追加）— **纯机制说明书**：读侧引导 + 写侧 kind 规范
   + 沉淀边界 + 回显格式 + **title 写清晰的硬要求**
   （注：**不含**任何项目铁律——铁律由各项目自己的项目根 CLAUDE.md 承载，见 §5）

## 7b. 配置部署方式 + 全局规则↔项目契约（已定稿 ✅）

部署方式：**全局规则一份，projectId 各项目自管。**

```
~/.claude/                          ← 全局，维护一份，所有项目共用
  ├─ settings.json                  注册 Stop hook（command 用绝对路径指向下面的脚本）
  ├─ hooks/memory-sink.js           写侧逻辑，全局共用
  └─ CLAUDE.md                      读写规则、kind 规范、全局铁律

<每个生产项目>/
  └─ .project-memory                ← 项目根的标记文件，存 projectId
```

职责边界（重要）：
- **全局 .claude（规则层）= 只读消费者**：只管"怎么用"，不管 projectId"怎么来"。
  读到 projectId 就用，读不到就放行，绝不自作主张生成。无状态、通用、永不需改。
- **各项目 = 自管 projectId 的产生和落地**：自己调 register_project 拿 id、自己写进
  标记文件。怎么生成/何时生成/谁来做，全局规则一概不管。

全局规则 ↔ 项目 的契约（定稿）：
- 文件名：`.project-memory`
- 位置：**工作空间根目录（Claude Code 启动时的当前目录），只在这一层找；
  不向上查找（避免越过工作空间边界、权限外读取）、不向下递归。**
- 格式：JSON —— `{ "projectId": "prj_xxxx" }`
  （用 JSON 是为留扩展位，将来可加项目级开关如 `"autoSink": false`）
- hook 行为：读到 → 用该 projectId；文件不存在 / 解析失败 → **直接放行**，不报错不生成。

由此，Stop hook 的放行（不打回）条件共两条：
1. 本次会话无代码改动（transcript 无 Edit/Write/MultiEdit）；或
2. 工作空间根目录无 `.project-memory`（当前不是接入了 memory 的项目）。
满足任一即放行；二者都不满足才打回做沉淀。

### 已知小问题：settings.json 里 hook 路径暂用绝对路径（待优化）

- 三个文件中**只有 `settings.json` 含路径**（CLAUDE.md 纯文本、memory-sink.cjs 内部无写死路径）。
- 当前 `command` 写死绝对路径：`node "C:\Users\Administrator\.claude\hooks\memory-sink.cjs"`。
- 想改成跨系统通用（相对/变量）但有坑，**暂不深究，先记下**：
  - hook command 的工作目录 = 用户启动 claude 的**项目目录**，不是 `~/.claude`
    → 直接写相对路径会相对到项目目录、找不到脚本，**否决**。
  - 可移植方案需借环境变量指向 home（unix `$HOME`，Windows 视所用 shell 而定），
    但 Windows 上 hook command 过 cmd 还是 sh、`~`/`$HOME` 是否展开**未实测、不确定**。
  - 结论：**当前先用绝对路径（最稳）**；若将来要一份 settings.json 跨 Mac/Linux/Windows，
    再实测各系统 shell 的变量展开行为，可能得接受"每系统这一行不同"。
- 脚本扩展名用 **`.cjs`**（非 .js）：避免落到带 `"type":"module"` 的目录时被当 ESM、
  `require` 报错；.cjs 强制 CommonJS，与所在目录的 package.json 无关。

## 8. 当前方案总览（一句话）

给 agent 装一个"会自我更新的项目长期记忆"：
- **写**：Stop hook 强制，任务结束（且有代码改动）时沉淀决策/教训/约定/示例，先查重再写，回显记录。
- **读**：CLAUDE.md 提示词引导，开工前纯语义 search_knowledge 召回；极少数全局铁律常驻 CLAUDE.md。
- **不做**：代码索引、知识图谱、tag 体系（均为过度设计 / 与"agent 能读源码"定位重复）。

## 9. 待讨论 / 未决问题（TODO）

已定：
- [x] 配置放哪 → 全局 `~/.claude` 一份规则；projectId 各项目自管（见 §7b）
- [x] projectId 怎么定位 → 工作空间根 `.project-memory`（JSON），不向上查找（见 §7b）
- [x] projectId 怎么生成 → 各项目自己负责，全局规则不管（见 §7b）

已定（本轮新增）：
- [x] 过滤逻辑细节 → **per-turn 判定**（只扫"本回合"，见 §4d），接受同一任务可能沉淀 2 次的冗余
- [x] 全局铁律清单 → **不归我们管**，删除（铁律由各项目自己的项目根 CLAUDE.md 承载，见 §5）

已定：
- [x] kind 取值最终敲定 → 5 个：decision/convention/pattern/gotcha/note + "一条只归一类"纪律（见 §3）
- [x] 沉淀记录回显格式最终敲定 → 见 §4（每条一行，仅"新增"带摘要行，四状态图标，带 ID）

待定：
- [ ] 生产项目领域是否同构（影响：将来若要补 tag，是一份通用词表还是一项目一份）—— 暂搁置，
      当前不上 tag，等真出现"搜不准"再说
